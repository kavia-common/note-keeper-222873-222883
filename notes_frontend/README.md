# Notes Frontend

Development:
- npm install
- npm run dev (runs on port 3000)

Configuration:
- Uses Vite.
- Backend base URL is http://localhost:3001 by default; override with VITE_API_BASE env var.

Auth:
- Stores bearer token in localStorage under key "token".
